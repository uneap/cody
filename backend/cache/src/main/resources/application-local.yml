spring:
  config:
    activate:
      on-profile: local
kafka:
  brand:
    retry:
      topic: brand-retry-stream
    topic: brand-stream
  product:
    retry:
      topic: product-retry-stream
    topic: product-stream